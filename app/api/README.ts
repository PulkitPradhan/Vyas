// Stable HTTP contract routes live here (e.g. /api/sms-webhook — Phase 12).
// Per ADR-001, mutation logic otherwise goes through Server Actions, not API routes.
export const dynamic = "force-dynamic";
