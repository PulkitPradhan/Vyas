import { describe, it, expect } from "vitest";
import { generateFlagExplanation } from "./gemini";

describe("generateFlagExplanation", () => {
  it("generates a flag explanation (stub test)", () => {
    // Tests for OpenRouter tool calls will be added for agent.ts
    // For now, this just asserts the module loads without error.
    expect(generateFlagExplanation).toBeDefined();
  });
});
